// ==========================================================================
// Standard timing / animation
// ==========================================================================

const Duration VALUE_ANIMATION_FAST = Duration(milliseconds: 200);
const Duration VALUE_ANIMATION_MEDIUM = Duration(milliseconds: 350);
const Duration VALUE_ANIMATION_SLOW = Duration(milliseconds: 600);

const Duration VALUE_RESEND_OTP_WAIT = Duration(seconds: 60);
const Duration VALUE_SNACKBAR_DURATION = Duration(seconds: 4);

// ==========================================================================
// Border Radius (raw doubles — for non-screenutil contexts)
// ==========================================================================

const double VALUE_RADIUS_SM = 8.0;
const double VALUE_RADIUS_MD = 12.0;
const double VALUE_RADIUS_LG = 16.0;
const double VALUE_RADIUS_XL = 24.0;
const double VALUE_RADIUS_PILL = 100.0;

const double VALUE_CARD_RADIUS = 16.0;
const double VALUE_CARD_RADIUS_LG = 24.0;
const double VALUE_BOTTOM_SHEET_RADIUS = 32.0;

// ==========================================================================
// Button Sizes (raw doubles)
// ==========================================================================

const double VALUE_BTN_HEIGHT = 56.0;
const double VALUE_BTN_HEIGHT_LG = 64.0;

// ==========================================================================
// Screen Layout
// ==========================================================================

const double VALUE_SCREEN_PADDING_H = 20.0;
const double VALUE_SECTION_SPACING = 24.0;

// ==========================================================================
// Figma Reference
// ==========================================================================

const double VALUE_FIGMA_DESIGN_WIDTH = 390.0;
const double VALUE_FIGMA_DESIGN_HEIGHT = 844.0;

// ==========================================================================
// Toast Durations (seconds)
// ==========================================================================

const int VALUE_TOAST_SHORT = 2;
const int VALUE_TOAST_MEDIUM = 3;
const int VALUE_TOAST_LONG = 5;

// ==========================================================================
// Image / Avatar Sizes
// ==========================================================================

const double VALUE_AVATAR_SIZE_SM = 32.0;
const double VALUE_AVATAR_SIZE_MD = 48.0;
const double VALUE_AVATAR_SIZE_LG = 72.0;
